diff --git a/tadac-app/.gitignore b/tadac-app/.gitignore
index 5ef6a52..24394fc 100644
--- a/tadac-app/.gitignore
+++ b/tadac-app/.gitignore
@@ -23,6 +23,9 @@
 # misc
 .DS_Store
 *.pem
+*.db
+*.sqlite
+migrations_backup_sqlite/
 
 # debug
 npm-debug.log*
diff --git a/tadac-app/prisma/dev.db b/tadac-app/prisma/dev.db
deleted file mode 100644
index 8b24497..0000000
Binary files a/tadac-app/prisma/dev.db and /dev/null differ
diff --git a/tadac-app/prisma/migrations/20260912170227_init/migration.sql b/tadac-app/prisma/migrations/20260912170227_init/migration.sql
deleted file mode 100644
index 1e392b8..0000000
--- a/tadac-app/prisma/migrations/20260912170227_init/migration.sql
+++ /dev/null
@@ -1,22 +0,0 @@
--- CreateTable
-CREATE TABLE "User" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "name" TEXT NOT NULL DEFAULT 'User',
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL
-);
-
--- CreateTable
-CREATE TABLE "Setting" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "theme" TEXT NOT NULL DEFAULT 'auto',
-    "timezone" TEXT NOT NULL DEFAULT 'Asia/Kolkata',
-    "quote" BOOLEAN NOT NULL DEFAULT true,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL,
-    CONSTRAINT "Setting_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- CreateIndex
-CREATE UNIQUE INDEX "Setting_userId_key" ON "Setting"("userId");
diff --git a/tadac-app/prisma/migrations/20260913142148_day2_full_schema/migration.sql b/tadac-app/prisma/migrations/20260913142148_day2_full_schema/migration.sql
deleted file mode 100644
index 4d59254..0000000
--- a/tadac-app/prisma/migrations/20260913142148_day2_full_schema/migration.sql
+++ /dev/null
@@ -1,307 +0,0 @@
-/*
-  Warnings:
-
-  - You are about to drop the column `quote` on the `Setting` table. All the data in the column will be lost.
-
-*/
--- CreateTable
-CREATE TABLE "Task" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "title" TEXT NOT NULL,
-    "description" TEXT NOT NULL DEFAULT '',
-    "scheduledDate" TEXT NOT NULL,
-    "scheduledTime" TEXT,
-    "priority" TEXT NOT NULL DEFAULT 'medium',
-    "category" TEXT NOT NULL DEFAULT 'Other',
-    "estimatedMins" INTEGER,
-    "deadline" TEXT,
-    "completed" BOOLEAN NOT NULL DEFAULT false,
-    "completedAt" DATETIME,
-    "sortOrder" INTEGER NOT NULL DEFAULT 0,
-    "xpAwarded" BOOLEAN NOT NULL DEFAULT false,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL,
-    CONSTRAINT "Task_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- CreateTable
-CREATE TABLE "FocusSession" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "taskId" TEXT,
-    "preset" TEXT NOT NULL DEFAULT '25/5',
-    "sessionType" TEXT NOT NULL DEFAULT 'focus',
-    "category" TEXT NOT NULL DEFAULT 'Other',
-    "plannedMins" INTEGER NOT NULL,
-    "actualMins" INTEGER NOT NULL DEFAULT 0,
-    "status" TEXT NOT NULL DEFAULT 'active',
-    "startedAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "endedAt" DATETIME,
-    "xpAwarded" BOOLEAN NOT NULL DEFAULT false,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    CONSTRAINT "FocusSession_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
-    CONSTRAINT "FocusSession_taskId_fkey" FOREIGN KEY ("taskId") REFERENCES "Task" ("id") ON DELETE SET NULL ON UPDATE CASCADE
-);
-
--- CreateTable
-CREATE TABLE "RevisionItem" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "topic" TEXT NOT NULL,
-    "notes" TEXT NOT NULL DEFAULT '',
-    "source" TEXT NOT NULL DEFAULT '',
-    "category" TEXT NOT NULL DEFAULT 'Other',
-    "tags" TEXT NOT NULL DEFAULT '',
-    "learnedAt" TEXT NOT NULL,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL,
-    CONSTRAINT "RevisionItem_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- CreateTable
-CREATE TABLE "RevisionCheckpoint" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "revisionItemId" TEXT NOT NULL,
-    "sequence" INTEGER NOT NULL,
-    "intervalDays" INTEGER NOT NULL,
-    "dueDate" TEXT NOT NULL,
-    "status" TEXT NOT NULL DEFAULT 'PENDING',
-    "completedAt" DATETIME,
-    "xpAwarded" BOOLEAN NOT NULL DEFAULT false,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL,
-    CONSTRAINT "RevisionCheckpoint_revisionItemId_fkey" FOREIGN KEY ("revisionItemId") REFERENCES "RevisionItem" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- CreateTable
-CREATE TABLE "InterviewQuestion" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "topic" TEXT NOT NULL,
-    "subTopic" TEXT NOT NULL DEFAULT '',
-    "difficulty" TEXT NOT NULL DEFAULT 'medium',
-    "format" TEXT NOT NULL DEFAULT 'mcq',
-    "question" TEXT NOT NULL,
-    "options" TEXT NOT NULL DEFAULT '[]',
-    "answer" TEXT NOT NULL,
-    "explanation" TEXT NOT NULL DEFAULT '',
-    "tags" TEXT NOT NULL DEFAULT '',
-    "active" BOOLEAN NOT NULL DEFAULT true,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL
-);
-
--- CreateTable
-CREATE TABLE "InterviewAttempt" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "questionId" TEXT NOT NULL,
-    "attemptDate" TEXT NOT NULL,
-    "userAnswer" TEXT NOT NULL,
-    "correct" BOOLEAN NOT NULL,
-    "xpAwarded" BOOLEAN NOT NULL DEFAULT false,
-    "attemptedAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    CONSTRAINT "InterviewAttempt_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
-    CONSTRAINT "InterviewAttempt_questionId_fkey" FOREIGN KEY ("questionId") REFERENCES "InterviewQuestion" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- CreateTable
-CREATE TABLE "Project" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "name" TEXT NOT NULL,
-    "description" TEXT NOT NULL DEFAULT '',
-    "difficulty" TEXT NOT NULL DEFAULT 'intermediate',
-    "durationEst" TEXT NOT NULL DEFAULT '',
-    "stack" TEXT NOT NULL DEFAULT '',
-    "categories" TEXT NOT NULL DEFAULT '',
-    "whatYouLearn" TEXT NOT NULL DEFAULT '',
-    "prerequisites" TEXT NOT NULL DEFAULT '',
-    "resources" TEXT NOT NULL DEFAULT '',
-    "active" BOOLEAN NOT NULL DEFAULT true
-);
-
--- CreateTable
-CREATE TABLE "ProjectStage" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "projectId" TEXT NOT NULL,
-    "sequence" INTEGER NOT NULL,
-    "title" TEXT NOT NULL,
-    "description" TEXT NOT NULL DEFAULT '',
-    "resources" TEXT NOT NULL DEFAULT '',
-    CONSTRAINT "ProjectStage_projectId_fkey" FOREIGN KEY ("projectId") REFERENCES "Project" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- CreateTable
-CREATE TABLE "ProjectStageProgress" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "projectId" TEXT NOT NULL,
-    "stageId" TEXT NOT NULL,
-    "status" TEXT NOT NULL DEFAULT 'NOT_STARTED',
-    "startedAt" DATETIME,
-    "completedAt" DATETIME,
-    "xpAwarded" BOOLEAN NOT NULL DEFAULT false,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL,
-    CONSTRAINT "ProjectStageProgress_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
-    CONSTRAINT "ProjectStageProgress_projectId_fkey" FOREIGN KEY ("projectId") REFERENCES "Project" ("id") ON DELETE CASCADE ON UPDATE CASCADE,
-    CONSTRAINT "ProjectStageProgress_stageId_fkey" FOREIGN KEY ("stageId") REFERENCES "ProjectStage" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- CreateTable
-CREATE TABLE "Reward" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "name" TEXT NOT NULL,
-    "description" TEXT NOT NULL DEFAULT '',
-    "requirement" TEXT NOT NULL DEFAULT '',
-    "redeemed" BOOLEAN NOT NULL DEFAULT false,
-    "redeemedAt" DATETIME,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL,
-    CONSTRAINT "Reward_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- CreateTable
-CREATE TABLE "JournalEntry" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "entryDate" TEXT NOT NULL,
-    "wentWell" TEXT NOT NULL DEFAULT '',
-    "wentBadly" TEXT NOT NULL DEFAULT '',
-    "learned" TEXT NOT NULL DEFAULT '',
-    "improveTomorrow" TEXT NOT NULL DEFAULT '',
-    "tomorrowPriorities" TEXT NOT NULL DEFAULT '[]',
-    "tomorrowExtras" TEXT NOT NULL DEFAULT '[]',
-    "xpAwarded" BOOLEAN NOT NULL DEFAULT false,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL,
-    CONSTRAINT "JournalEntry_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- CreateTable
-CREATE TABLE "XpEvent" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "amount" INTEGER NOT NULL,
-    "reason" TEXT NOT NULL,
-    "sourceId" TEXT NOT NULL,
-    "earnedAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    CONSTRAINT "XpEvent_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-
--- RedefineTables
-PRAGMA defer_foreign_keys=ON;
-PRAGMA foreign_keys=OFF;
-CREATE TABLE "new_Setting" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "userId" TEXT NOT NULL,
-    "theme" TEXT NOT NULL DEFAULT 'auto',
-    "timezone" TEXT NOT NULL DEFAULT 'Asia/Kolkata',
-    "focusPreset" TEXT NOT NULL DEFAULT '25/5',
-    "customFocusMins" INTEGER NOT NULL DEFAULT 25,
-    "customShortBreak" INTEGER NOT NULL DEFAULT 5,
-    "customLongBreak" INTEGER NOT NULL DEFAULT 15,
-    "customCycles" INTEGER NOT NULL DEFAULT 4,
-    "autoStartBreak" BOOLEAN NOT NULL DEFAULT false,
-    "autoStartFocus" BOOLEAN NOT NULL DEFAULT false,
-    "quoteEnabled" BOOLEAN NOT NULL DEFAULT true,
-    "quoteCategories" TEXT NOT NULL DEFAULT 'stoic,discipline,focus',
-    "xpFocusSession" INTEGER NOT NULL DEFAULT 30,
-    "xpTask" INTEGER NOT NULL DEFAULT 10,
-    "xpInterview" INTEGER NOT NULL DEFAULT 10,
-    "xpRevision" INTEGER NOT NULL DEFAULT 15,
-    "xpProjectMilestone" INTEGER NOT NULL DEFAULT 50,
-    "xpDailyPlanning" INTEGER NOT NULL DEFAULT 10,
-    "xpJournal" INTEGER NOT NULL DEFAULT 10,
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL,
-    CONSTRAINT "Setting_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User" ("id") ON DELETE CASCADE ON UPDATE CASCADE
-);
-INSERT INTO "new_Setting" ("createdAt", "id", "theme", "timezone", "updatedAt", "userId") SELECT "createdAt", "id", "theme", "timezone", "updatedAt", "userId" FROM "Setting";
-DROP TABLE "Setting";
-ALTER TABLE "new_Setting" RENAME TO "Setting";
-CREATE UNIQUE INDEX "Setting_userId_key" ON "Setting"("userId");
-CREATE TABLE "new_User" (
-    "id" TEXT NOT NULL PRIMARY KEY,
-    "name" TEXT NOT NULL DEFAULT 'Parth',
-    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
-    "updatedAt" DATETIME NOT NULL
-);
-INSERT INTO "new_User" ("createdAt", "id", "name", "updatedAt") SELECT "createdAt", "id", "name", "updatedAt" FROM "User";
-DROP TABLE "User";
-ALTER TABLE "new_User" RENAME TO "User";
-PRAGMA foreign_keys=ON;
-PRAGMA defer_foreign_keys=OFF;
-
--- CreateIndex
-CREATE INDEX "Task_userId_scheduledDate_idx" ON "Task"("userId", "scheduledDate");
-
--- CreateIndex
-CREATE INDEX "Task_userId_completed_idx" ON "Task"("userId", "completed");
-
--- CreateIndex
-CREATE INDEX "FocusSession_userId_startedAt_idx" ON "FocusSession"("userId", "startedAt");
-
--- CreateIndex
-CREATE INDEX "FocusSession_userId_status_idx" ON "FocusSession"("userId", "status");
-
--- CreateIndex
-CREATE INDEX "RevisionItem_userId_idx" ON "RevisionItem"("userId");
-
--- CreateIndex
-CREATE UNIQUE INDEX "RevisionItem_userId_topic_learnedAt_key" ON "RevisionItem"("userId", "topic", "learnedAt");
-
--- CreateIndex
-CREATE INDEX "RevisionCheckpoint_revisionItemId_idx" ON "RevisionCheckpoint"("revisionItemId");
-
--- CreateIndex
-CREATE INDEX "RevisionCheckpoint_dueDate_status_idx" ON "RevisionCheckpoint"("dueDate", "status");
-
--- CreateIndex
-CREATE UNIQUE INDEX "RevisionCheckpoint_revisionItemId_sequence_key" ON "RevisionCheckpoint"("revisionItemId", "sequence");
-
--- CreateIndex
-CREATE INDEX "InterviewQuestion_topic_active_idx" ON "InterviewQuestion"("topic", "active");
-
--- CreateIndex
-CREATE INDEX "InterviewQuestion_difficulty_idx" ON "InterviewQuestion"("difficulty");
-
--- CreateIndex
-CREATE INDEX "InterviewAttempt_userId_attemptDate_idx" ON "InterviewAttempt"("userId", "attemptDate");
-
--- CreateIndex
-CREATE INDEX "InterviewAttempt_userId_questionId_idx" ON "InterviewAttempt"("userId", "questionId");
-
--- CreateIndex
-CREATE UNIQUE INDEX "InterviewAttempt_userId_questionId_attemptDate_key" ON "InterviewAttempt"("userId", "questionId", "attemptDate");
-
--- CreateIndex
-CREATE INDEX "Project_difficulty_active_idx" ON "Project"("difficulty", "active");
-
--- CreateIndex
-CREATE INDEX "ProjectStage_projectId_idx" ON "ProjectStage"("projectId");
-
--- CreateIndex
-CREATE UNIQUE INDEX "ProjectStage_projectId_sequence_key" ON "ProjectStage"("projectId", "sequence");
-
--- CreateIndex
-CREATE INDEX "ProjectStageProgress_userId_projectId_idx" ON "ProjectStageProgress"("userId", "projectId");
-
--- CreateIndex
-CREATE UNIQUE INDEX "ProjectStageProgress_userId_stageId_key" ON "ProjectStageProgress"("userId", "stageId");
-
--- CreateIndex
-CREATE INDEX "Reward_userId_idx" ON "Reward"("userId");
-
--- CreateIndex
-CREATE INDEX "JournalEntry_userId_idx" ON "JournalEntry"("userId");
-
--- CreateIndex
-CREATE UNIQUE INDEX "JournalEntry_userId_entryDate_key" ON "JournalEntry"("userId", "entryDate");
-
--- CreateIndex
-CREATE INDEX "XpEvent_userId_earnedAt_idx" ON "XpEvent"("userId", "earnedAt");
-
--- CreateIndex
-CREATE UNIQUE INDEX "XpEvent_userId_reason_sourceId_key" ON "XpEvent"("userId", "reason", "sourceId");
diff --git a/tadac-app/prisma/migrations/migration_lock.toml b/tadac-app/prisma/migrations/migration_lock.toml
index e5e5c47..fbffa92 100644
--- a/tadac-app/prisma/migrations/migration_lock.toml
+++ b/tadac-app/prisma/migrations/migration_lock.toml
@@ -1,3 +1,3 @@
 # Please do not edit this file manually
 # It should be added in your version-control system (i.e. Git)
-provider = "sqlite"
\ No newline at end of file
+provider = "postgresql"
\ No newline at end of file
diff --git a/tadac-app/prisma/schema.prisma b/tadac-app/prisma/schema.prisma
index c5cd0a1..cc1f8e5 100644
--- a/tadac-app/prisma/schema.prisma
+++ b/tadac-app/prisma/schema.prisma
@@ -6,7 +6,7 @@ generator client {
 }
 
 datasource db {
-  provider = "sqlite"
+  provider = "postgresql"
   url      = env("DATABASE_URL")
 }
 
diff --git a/tadac-app/vitest.config.ts b/tadac-app/vitest.config.ts
index af355ac..676cae4 100644
--- a/tadac-app/vitest.config.ts
+++ b/tadac-app/vitest.config.ts
@@ -7,7 +7,7 @@ export default defineConfig({
     environment: 'node',
     setupFiles: ['./vitest.setup.ts'],
     env: {
-      DATABASE_URL: 'file:./test.db'
+      DATABASE_URL: process.env.TEST_DATABASE_URL || ''
     },
     fileParallelism: false
   },
diff --git a/tadac-app/vitest.setup.ts b/tadac-app/vitest.setup.ts
index 54960b1..b52072a 100644
--- a/tadac-app/vitest.setup.ts
+++ b/tadac-app/vitest.setup.ts
@@ -9,25 +9,32 @@ vi.mock('next/cache', () => ({
 }));
 
 beforeAll(() => {
-  // Push the schema to the test database
-  console.log("Setting up test database at ./test.db...");
+  // Push the schema to the Postgres test database natively
+  console.log("Setting up Postgres test database at tadac_test...");
   execSync('npx prisma db push --accept-data-loss', { 
     stdio: 'ignore', 
-    env: { ...process.env, DATABASE_URL: 'file:./test.db' }
+    env: { ...process.env, DATABASE_URL: process.env.TEST_DATABASE_URL }
   });
 });
 
 afterAll(async () => {
   await prisma.$disconnect();
-  // Optional: Clean up test.db
+  // Truncate tables natively to avoid physical file destruction issues
   try {
-    if (fs.existsSync('./prisma/test.db')) {
-      fs.unlinkSync('./prisma/test.db');
-    }
-    if (fs.existsSync(path.join(process.cwd(), 'test.db'))) {
-      fs.unlinkSync(path.join(process.cwd(), 'test.db'));
+    const tableNames = await prisma.$queryRaw<
+      Array<{ tablename: string }>
+    >`SELECT tablename FROM pg_tables WHERE schemaname='public'`;
+
+    const tables = tableNames
+      .map(({ tablename }: { tablename: string }) => tablename)
+      .filter((name: string) => name !== '_prisma_migrations')
+      .map((name: string) => `"public"."${name}"`)
+      .join(', ');
+
+    if (tables !== '') {
+      await prisma.$executeRawUnsafe(`TRUNCATE TABLE ${tables} CASCADE;`);
     }
   } catch (e) {
-    console.log("Could not unlink test.db (file locked), skipping teardown");
+    console.log("Could not truncate Postgres tables gracefully natively:", e);
   }
 });
